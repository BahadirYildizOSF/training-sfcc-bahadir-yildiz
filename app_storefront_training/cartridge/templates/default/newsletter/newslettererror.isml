<isdecorate template="account/pt_account">
    <h4>${Resource.msg("newsletter.error.header", "locale", null)}</h4>
    ${Resource.msgf("newsletter.error.description", "locale", null, pdict.CurrentForms.newsletter.email.value, URLUtils.url('Newsletter-Start'))}
    Maybe the email ${pdict.CurrentForms.newsletter.email.value} already exists?
    Back to <a href="${URLUtils.url('Newsletter-Start')}">Newsletter Subscription</a>.
    ${pdict.error}
</isdecorate>